bin/
obj/
*.user
.vs/

# Build/publish output — regenerated by installer/build-punters-installer.ps1, never
# hand-edited, and far too large to check in.
installer/publish-punters/
installer/output/

# Server-local secrets — written by the app itself at runtime, never meant to leave the machine
# they're on.
settings.json
