bin/
obj/
*.user
.vs/

# Build/publish output — regenerated by installer/build-punters-installer.ps1 and
# deploy/publish-web.ps1, never hand-edited, and far too large to check in.
installer/publish-punters/
installer/output/
deploy/publish-web/
deploy/*.zip

# Server-local secrets — written by deploy/server-setup.ps1 or the apps themselves at runtime,
# never meant to leave the machine they're on.
appsettings.Production.json
App_Data/
settings.json
